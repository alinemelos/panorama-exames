services:
  db-panorama-exames:
    image: imresamu/postgis:17-3.5 #MacOs
    #image: postgis/postgis:17-3.5 #Linux
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
